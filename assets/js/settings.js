'use strict';

const priorityClassesMap = new Map([
    [0, "priority-low"],
    [1, "priority-medium"],
    [2, "priority-high"]
]);

const settings = {
    priorityClassesMap,
}

export default settings;
